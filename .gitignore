# Terraform directories and state files
**/.terraform/
*.tfstate
*.tfstate.backup
*.tfstate.*

# Terragrunt cache
.terragrunt-cache/

# Terraform and Terragrunt lock files
.terraform.lock.hcl
terraform.tfstate.lock.info

# Terragrunt-generated files (e.g., generate blocks)
# Typically already covered by terragrunt-cache unless custom paths used

# *.tf generated? Usually avoid ignoring all .tf—only auto-generated ones if needed

# Variable and override files (often contain secrets)
*.tfvars
*.tfvars.json
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Crash logs and plan outputs
crash.log
crash.*.log
# Uncomment if you use plan files
# *tfplan*

# CLI and environment config
.terraformrc
terraform.rc
.env
.terragrunt-stack

# IDE and OS artifacts
*.sw?
.idea/
.idea_modules/
*.iml
*.iws
*.ipr
.DS_Store
.history
.vscode/
build/
*/build/
out/

# Deployment artifacts (e.g., zip files)
*.zip

